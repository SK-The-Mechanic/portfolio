@AGENTS.md
<!-- landing page is done. now it's time for the next part which is work and the twist start from here now we need two part one is for client and other is for admin only one admin. as you can see on my html code I hardcoded the work but now I need an admin side where I can put a link and the project name if any user clicked on the project name it will land them on the project link and they can see the project. in admin panel we need many more things to add like if any user fill the form of contact use admin can see this and after that admin also can delete this. admin can create a code and that's only work for one time why the code is for? if any client don't need to create an account just to make a comment they can use the code just for one time to make a comment. admin can create coupon code discounted coupon code for individual user for all user and one coupon only work for one time then it delete from database automatically admin can delete the coupon from admin panel deleting means deactivate plus delete from the database. admin can update the contacts link number mail. but honestly right now just wanna do the work part okay buddy. -->